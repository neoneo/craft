# This is the test routes file for the integration test.

GET		/											Root
GET		/artists									ArtistList
POST	.											Artist # POST goes to /artists

# /artists/id
GET		> /[0-9]+@artist 							Artist
PUT		.											Artist
DELETE	.											Artist
GET		>> /picture@action							Artist

# /artists/id/albums
GET		>> /albums									AlbumList
POST	.											Album

# /artists/id/albums/id
GET		>>> /[0-9]+@album							Album
PUT		.											Album
DELETE	.											Album

# /artists/id/albums/id/songs
GET		>>>> /songs									SongList
POST	.											Song

# /artists/id/albums/id/songs/id
GET		>>>>> /[0-9]+@song							Song
PUT		.											Song
DELETE	.											Song