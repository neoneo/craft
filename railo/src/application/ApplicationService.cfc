component accessors="true" {

/*
	property Builder builder setter="false";

	public void function init(required String rootFolder) {

		var folder = ExpandPath(arguments.rootFolder)
		var nodeFactory = new NodeFactory()
		var contentFactory = new ContentFactory(nodeFactory = nodeFactory)

		variables.builder = new Builder(rootFolder = folder, contentFactory = contentFactory)

		// build the whole application
		variables.pathSegment = variables.builder.build()

	}

*/

}